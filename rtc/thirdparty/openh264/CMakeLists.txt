PROJECT(openh264)

IF(ANDROID)
	SET(CAN_USE_ASSEMBLER TRUE)
	ENABLE_LANGUAGE(ASM)
ELSE()
	SET(CMAKE_ASM_COMPILE_OBJECT yasm)
ENDIF(ANDROID)


ADD_SUBDIRECTORY(codec/common)
ADD_SUBDIRECTORY(codec/processing)
ADD_SUBDIRECTORY(codec/encoder)

